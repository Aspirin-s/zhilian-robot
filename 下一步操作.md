# 🚀 下一步操作指引

## ✅ 已完成的修改

### 1. 代码修改
- ✅ `backend/config/settings.py` - 添加 DeepSeek API Base URL 配置
- ✅ `backend/app/nlp/llm.py` - 修改 LLM 处理器以使用 DeepSeek
- ✅ `backend/.env` - 配置您的 DeepSeek API 密钥
- ✅ `backend/.env.example` - 更新环境变量模板

### 2. 新增文档
- ✅ `docs/DeepSeek配置说明.md` - 详细的 DeepSeek 使用指南
- ✅ `backend/test_deepseek.py` - API 连接测试脚本

### 3. 配置信息
```env
OPENAI_API_KEY=sk-08266faa1d184709878869666545ea9a
OPENAI_API_BASE=https://api.deepseek.com
OPENAI_MODEL=deepseek-chat
```

## 📋 下一步操作清单

### 步骤 1: 测试 DeepSeek API 连接 (可选但推荐)

在虚拟环境中运行测试脚本:

```powershell
# 确保在虚拟环境中
cd D:\Codefield\rgg\zhilian-robot
.\backend\venv\Scripts\Activate.ps1

# 运行测试
cd backend
python test_deepseek.py
```

**预期结果**:
- ✅ 基础连接测试通过
- ✅ 实体提取测试通过
- 显示 Token 使用情况和提取结果

### 步骤 2: 重新构建 Docker 镜像

由于修改了代码,需要重新构建:

```powershell
# 回到项目根目录
cd D:\Codefield\rgg\zhilian-robot

# 重新构建后端镜像
docker-compose build backend
```

**等待时间**: 约 5-10 分钟(主要是下载 PyTorch)

### 步骤 3: 启动所有服务

```powershell
# 启动所有容器
docker-compose up -d
```

**启动的服务**:
- 🗄️ Neo4j (图数据库) - 端口 7474, 7687
- 🗄️ MongoDB (文档数据库) - 端口 27017
- 🗄️ Redis (缓存) - 端口 6379
- 🗄️ MySQL (关系数据库) - 端口 3306
- 🔧 Backend (FastAPI) - 端口 8000
- 🌐 Frontend (React) - 端口 3000

### 步骤 4: 验证服务状态

```powershell
# 查看容器运行状态
docker-compose ps

# 查看日志(如有问题)
docker-compose logs -f backend
```

### 步骤 5: 访问应用

打开浏览器访问:

1. **前端应用**: http://localhost:3000
   - 主页
   - 知识图谱查询
   - 文本分析

2. **后端 API 文档**: http://localhost:8000/docs
   - 交互式 API 文档
   - 可直接测试所有接口

3. **Neo4j 浏览器**: http://localhost:7474
   - 用户名: neo4j
   - 密码: password (需在 .env 中配置)

### 步骤 6: 测试 DeepSeek 功能

在前端或 API 文档中测试:

#### 方法 A: 使用 API 文档测试

1. 访问 http://localhost:8000/docs
2. 找到 `/api/v1/nlp/analyze-llm` 端点
3. 点击 "Try it out"
4. 输入测试文本:
   ```
   小米公司发布了新款智能机器人,采用了自研的AI芯片和视觉识别技术
   ```
5. 点击 "Execute"
6. 查看返回的实体和关系提取结果

#### 方法 B: 使用前端界面测试

1. 访问 http://localhost:3000
2. 进入"文本分析"页面
3. 输入测试文本
4. 点击"分析"按钮
5. 查看可视化的实体和关系结果

## 🔍 故障排查

### 问题 1: Docker 构建失败

**解决方案**:
```powershell
# 清理缓存重新构建
docker-compose build --no-cache backend
```

### 问题 2: 容器启动失败

**检查日志**:
```powershell
docker-compose logs backend
```

**常见原因**:
- 端口占用: 确保 8000 端口未被占用
- 依赖问题: 检查 requirements.txt 是否正确

### 问题 3: DeepSeek API 调用失败

**检查清单**:
- [ ] API Key 是否正确
- [ ] 网络是否能访问 api.deepseek.com
- [ ] DeepSeek 账户是否有余额
- [ ] 是否超过 API 限流

**测试网络连接**:
```powershell
curl https://api.deepseek.com/v1/models `
  -H "Authorization: Bearer sk-08266faa1d184709878869666545ea9a"
```

### 问题 4: 数据库连接失败

**修改 .env 中的数据库密码**:
```env
NEO4J_PASSWORD=your_secure_password
MYSQL_PASSWORD=your_secure_password
```

**重启服务**:
```powershell
docker-compose down
docker-compose up -d
```

## 📊 监控和日志

### 查看实时日志

```powershell
# 所有服务
docker-compose logs -f

# 仅后端
docker-compose logs -f backend

# 仅前端
docker-compose logs -f frontend
```

### 查看资源使用

```powershell
docker stats
```

## 🎯 快速命令参考

```powershell
# 启动服务
docker-compose up -d

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 查看状态
docker-compose ps

# 进入后端容器
docker-compose exec backend bash

# 查看日志
docker-compose logs -f backend
```

## 📚 相关文档

- [DeepSeek 配置说明](./docs/DeepSeek配置说明.md)
- [快速开始指南](./docs/快速开始.md)
- [开发文档](./docs/开发文档.md)
- [用户操作手册](./docs/用户操作手册.md)

## ✨ 成功标志

当您看到以下情况时,说明系统运行成功:

- ✅ `docker-compose ps` 显示所有服务状态为 "Up"
- ✅ 访问 http://localhost:3000 看到前端界面
- ✅ 访问 http://localhost:8000/docs 看到 API 文档
- ✅ 在 API 文档中成功调用 `/api/v1/nlp/analyze-llm` 端点
- ✅ 返回的 JSON 包含提取的实体和关系

## 🎉 恭喜!

完成以上步骤后,您的智链机器人系统就可以:
- 🤖 使用 DeepSeek AI 进行智能文本分析
- 📊 构建产业链知识图谱
- 🔍 进行实体和关系提取
- 🌐 通过 Web 界面交互使用

---

**当前位置**: 代码修改完成,准备测试和部署

**下一步**: 执行步骤 2 - 重新构建 Docker 镜像
